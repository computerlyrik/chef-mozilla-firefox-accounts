# description "Start the fxa-auth-server"

start on runlevel [2345]
stop on runlevel [^2345]

console log
chdir <%= node['mozilla-firefox-accounts']['auth-server']['path'] %>
setuid fxcloud
setgid fxcloud

env NODE_ENV=prod 
respawn
respawn limit 20 5

exec npm start
